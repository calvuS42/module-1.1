/*#include <iostream>
#include <cstdlib>
#include <cmath>

using namespace std;

double polyCount(double a, double b, double c, double x) {
	return a*x*x + b*x + c;
}

void main() {
	cout << "enter a, b, c! \n";
	double a, b, c, x;
	cin >> a >> b >> c;
	int i = 1;
	while (i) {
		cout << "enter x! \n";
		cin >> x;
		if (x == 0) i = 0;
		cout << polyCount(a, b, c, x) << endl;
	}
	system("pause");
}*/